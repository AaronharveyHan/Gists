{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "webdriver",
  "description": "Permissions required by the WDIO Tauri plugin (debug builds only).",
  "windows": ["main"],
  "permissions": [
    "wdio:default"
  ]
}
